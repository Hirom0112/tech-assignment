node_modules/
.serverless/
.env
dist/
.build/
coverage/
*.log
.DS_Store

# Reference-only design mockup (not shipped)
serverless-v2/services/streaks-frontend/public/assets/dashboard-ref.png

# Raw high-res art originals — vendored locally so they travel with the working
# tree, but kept out of the clean submission (web-ready versions live under
# public/assets/dashboard/). See HANDOFFS + memory [[hijack-poker-asset-sources]].
design-assets/

# Internal company/competitive deep-dive — kept local, not part of the public PR.
hijack-poker-company-dossier.md

# Internal build process / planning — kept local so the submission repo stays
# navigable for reviewers. The engineering doc suite (ARCHITECTURE / API_CONTRACT /
# DATA_MODEL / PROJECT / TECH_STACK / ASSUMPTIONS / RESEARCH) and the README ARE
# the deliverable; the slice-by-slice build logs and planning trackers are not.
SLICE_REPORTS/
HANDOFFS/
HANDOFF.md
PLAN.md
TODO.md
